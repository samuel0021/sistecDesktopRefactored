<Window x:Class="sistecDesktopRefactored.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:sistecDesktopRefactored"
        xmlns:viewModels="clr-namespace:sistecDesktopRefactored.ViewModels"
        mc:Ignorable="d"
        Title="LoginWindow" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen">
    
    <Grid Background="{StaticResource gradient-background}">
        <!--#region Column Definitions-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--#endregion-->

        <Border Width="500" Height="600" CornerRadius="10"
            HorizontalAlignment="Right" Margin="10"
            Style="{StaticResource BorderShadow}"
            Background="{StaticResource brush-vibrantViolet}">

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="/images/logo-white.png" 
                    Width="100" Height="100" Margin="0 0 0 0"/>

                <TextBlock Text="Sistema de Chamados"
                    Margin="0 30 0 10"
                    TextAlignment="Center" FontSize="16"
                    FontFamily="{StaticResource MontserratBold}"
                    Foreground="{StaticResource brush-lavenderMist}"/>

                <TextBlock Text="Gerencie seus chamados técnicos de forma eficiente"
                    TextWrapping="Wrap" TextAlignment="Center"
                    FontSize="10"
                    Foreground="{StaticResource brush-lavenderMist}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" HorizontalAlignment="Left"
            Width="500" Height="600" CornerRadius="10" Margin="10"
            Tag="Login"
            Style="{StaticResource BorderShadow}"
            Background="{StaticResource brush-lavenderMist}">

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Entrar no Sistema"
                    TextAlignment="Center" FontSize="18"
                    Margin="0 15 0 5"
                    FontFamily="{StaticResource MontserratBold}"
                    Foreground="{StaticResource brush-midnightNavy}"/>

                <TextBlock Text="Digite suas credenciais para acessar"
                    TextAlignment="Center" FontSize="12"
                    Margin="0 0 0 10"
                    Foreground="{StaticResource brush-coolGray}"/>


                <!-- Email -->
                <Label Content="E-mail" 
                    Foreground="{StaticResource brush-midnightNavy}"/>

                <TextBox x:Name="txtEmail"
                    Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                    Height="25" Margin="5 0 5 0"/>

                <!-- Senha -->
                <Label Content="Senha" Margin="0 10 0 0"
                    Foreground="{StaticResource brush-midnightNavy}"/>

                <PasswordBox x:Name="passwordBox"
                    PasswordChar="●" 
                    Height="25"
                    Margin="5 0 5 0"
                    PasswordChanged="PasswordBox_PasswordChanged"/>

                <TextBlock Margin="0 10 10 10"
                    HorizontalAlignment="Right"
                    FontSize="12">
                
                <Hyperlink Command="{Binding EsqueciSenhaCommand}"
                    Style="{StaticResource HyperlinkForgotPass}">
                    Esqueceu sua senha?
                 </Hyperlink>
                </TextBlock>

                <!-- Botão Entrar -->
                <Button x:Name="btnEnter"
                    FontFamily="{StaticResource MontserratBold}"
                    Content="Entrar" FontSize="12" 
                    Height="25" Margin="20, 5, 20, 0"
                    IsDefault="True"
                    Command="{Binding LoginCommand}"/>


                <!-- Mensagem de Erro (sempre visível, mas só tem texto se der erro) -->
                <TextBlock Text="{Binding ErrorMessage}"
                    Foreground="Red"
                    TextWrapping="Wrap"
                    TextAlignment="Center"
                    FontSize="10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
