<UserControl x:Class="sistecDesktopRefactored.Views.TicketCreatedMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sistecDesktopRefactored.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:sistecDesktopRefactored.Converters"
             xmlns:helpers="clr-namespace:sistecDesktopRefactored.Helpers"
             mc:Ignorable="d" 
             d:DesignWidth="450" d:DesignHeight="325"
             Width="450" Height="325">

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Border Background="{StaticResource brush-lavenderMist}"
            BorderBrush="#22C560"
            BorderThickness="1"
            CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Sucesso!" FontSize="26" Margin="20"
                   Style="{StaticResource MyTextblock}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

            <Image Grid.Row="1"
                Source="/Images/circle-check-big.png"/>

            <StackPanel Grid.Row="2" Margin="20">
                <TextBlock Text="{Binding TicketMessage}"
                    Style="{StaticResource MyTextblock}"
                    FontSize="20" Margin="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
                
                <TextBlock Text="Status: Aguardando aprovação do gestor."
                    Style="{StaticResource MyTextblock}"
                    FontSize="18" Margin="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Row="3"
                Content="Ok"
                Background="#22C55E"
                Height="50" Margin="10"
                Tag="/Images/circle-check.png"
                HorizontalAlignment="Right"
                FontFamily="{StaticResource MontserratBold}"
                Foreground="{StaticResource brush-White}"
                BorderBrush="{StaticResource brush-White}"
                helpers:ButtonHelper.HoverBackground="#22C560"
                Style="{StaticResource ButtonImage}"
                Command="{Binding OkCommand}"/>
        </Grid>
    </Border>
</UserControl>
