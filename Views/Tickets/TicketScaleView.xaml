<UserControl x:Class="sistecDesktopRefactored.Views.Tickets.TicketScaleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sistecDesktopRefactored.Views.Tickets"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:sistecDesktopRefactored.Converters"
             xmlns:helpers="clr-namespace:sistecDesktopRefactored.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="550">

    <!--#region Prism Window Styles-->
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <!--#endregion-->

    <Border Background="{StaticResource brush-lavenderMist}"
        CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource brush-peachOrange}"
                CornerRadius="10 10 0 0">

                <TextBlock Text="{Binding Title}"
                    Style="{StaticResource MyTextblock}"
                    Foreground="{StaticResource brush-White}"
                    FontSize="24" Margin="20"
                    VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Motivo do escalonamento:"
                    FontSize="18" Margin="15 0 0 5"
                    FontFamily="{StaticResource MontserratRegular}"/>

                <TextBox Text="{Binding Motivo, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource TextBoxBase}"
                    VerticalContentAlignment="Top" Padding="5"
                    Height="100"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                HorizontalAlignment="Center" Margin="20">

                <Button Content="Cancelar" 
                    Width="250" 
                    Height="40"
                    Margin="0,0,10,0"
                    BorderThickness="0"
                    Tag="/Images/x.png"
                    helpers:ButtonHelper.HoverBackground="#4FEC0000"
                    Foreground="{StaticResource brush-midnightNavy}"
                    FontFamily="{StaticResource MontserratBold}"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ButtonImage}"/>

                <Button Content="Resolver Chamado" 
                    Width="250" 
                    Height="40"
                    Background="{StaticResource brush-peachOrange}"
                    Foreground="White"
                    BorderThickness="0"
                    Tag="/Images/circle-check.png"
                    helpers:ButtonHelper.HoverBackground="#BCFF9966"
                    FontFamily="{StaticResource MontserratBold}"
                    Command="{Binding ScaleTicketCommand}"
                    Style="{StaticResource ButtonImage}"/>

            </StackPanel>

        </Grid>
    </Border>
</UserControl>
