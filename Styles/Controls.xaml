<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:sistecDesktopRefactored.Helpers">
    
    <!--#region Buttons-->

    <Style x:Key="ButtonBaseImage" TargetType="ButtonBase">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="borderButtonSolid"
                        CornerRadius="5" 
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" 
                        Width="{TemplateBinding Width}" 
                        Height="{TemplateBinding Height}" 
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        BorderBrush="{TemplateBinding BorderBrush}">

                        <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                            
                            <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                Width="20" Height="20" Margin="15 0 0 0"/>
                            
                            <TextBlock Text="{TemplateBinding Content}"
                                TextAlignment="Center" 
                                VerticalAlignment="Center" 
                                Margin="15"
                                Foreground="{TemplateBinding Foreground}"
                                FontFamily="{TemplateBinding FontFamily}" 
                                FontSize="{TemplateBinding FontSize}"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Background" TargetName="borderButtonSolid"
                                Value="{Binding (helpers:ButtonHelper.HoverBackground),
                            RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.025" Duration="0:0:0.1"/>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.025" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation 
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation 
                                            Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonImage"
       TargetType="Button"
       BasedOn="{StaticResource ButtonBaseImage}">
    </Style>

    <Style x:Key="ToggleButtonImage"
       TargetType="ToggleButton"
       BasedOn="{StaticResource ButtonBaseImage}">
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="Red"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--#endregion--> 
    
    <!--#region TextInputs-->
    <!-- Base -->
    <Style x:Key="TextInputBase" TargetType="Control">
        <Setter Property="Height" Value="40"/>
        <Setter Property="Margin" Value="15 0 15 0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="8 0 8 0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="FontFamily" Value="{DynamicResource MontserratRegular}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource brush-frenchGray}"/>
        <Setter Property="Foreground" Value="{DynamicResource brush-midnightNavy}"/>
    </Style>

    <!-- TextBox -->
    <Style x:Key="TextBoxBase"
       TargetType="TextBox"
       BasedOn="{StaticResource TextInputBase}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                        CornerRadius="5"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost"
                                  Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- PasswordBox -->
    <Style x:Key="PasswordBoxBase" TargetType="PasswordBox"
       BasedOn="{StaticResource TextInputBase}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="PasswordChar" Value="●"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="border"
                        CornerRadius="5"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost"
                            Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!--#endregion--> 

    <!--#region Borders-->

    <Style x:Key="BorderPanel" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource brush-lavenderMist}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource brush-frenchGray}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <Style x:Key="BorderShadow" TargetType="Border">
        <Setter Property="Effect">
            <Setter.Value >
                <DropShadowEffect Color="#000033"
                          BlurRadius="10"
                          ShadowDepth="5"
                          Direction="315"
                          Opacity="0.5"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <!-- #region Hyperlink-->
    
    <Style x:Key="HyperlinkForgotPass" TargetType="Hyperlink">
        <Setter Property="Foreground" Value="{DynamicResource brush-vibrantViolet}"/>
        <Setter Property="TextDecorations" Value="None"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="TextDecorations" Value="Underline"/>
            </Trigger>
            <!--<Trigger Property="IsMouseCaptured" Value="True">
            <Setter Property="Foreground" Value="{DynamicResource brush-tropicalIndigo}"/>
        </Trigger>-->
        </Style.Triggers>
    </Style>
    
    <!--#endregion-->
</ResourceDictionary>